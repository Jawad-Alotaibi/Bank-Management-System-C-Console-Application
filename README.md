# Bank-Management-System-C-Console-Application
A console-based Bank Management System built in C++. It manages client records via a menu-driven interface, storing data in a text file using a custom delimiter. Supports add, update, delete, and search with validation and modular design.
